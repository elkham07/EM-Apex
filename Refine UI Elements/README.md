
  # Refine UI Elements

  This is a code bundle for Refine UI Elements. The original project is available at https://www.figma.com/design/d34uHac6Wrm1KyCWGB1doG/Refine-UI-Elements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  